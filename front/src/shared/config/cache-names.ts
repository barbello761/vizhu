/**
 * Имена кэшей Cache Storage, которыми управляет service worker.
 *
 * Модуль намеренно без зависимостей: его импортирует не только приложение, но
 * и vite.config.ts, где правило runtimeCaching задаёт это же имя. Держим его
 * в одном месте, иначе очистка при выходе молча промахнётся мимо кэша.
 */

/** Ответы API, которые Workbox кладёт по NetworkFirst-правилу. */
export const API_CACHE_NAME = 'api-cache';

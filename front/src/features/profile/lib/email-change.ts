/**
 * Смена электронной почты — точка подключения бэкенда.
 *
 * Почта нигде не сохраняется: колонки в БД нет, писем слать нечем. Экраны
 * флоу («Введите код» → «Введите новую почту» → «Проверьте почту» →
 * «Электронная почта изменена») свёрстаны целиком и ходят сюда, так что
 * подключение сводится к замене тел этих двух функций на запросы.
 *
 * Ожидаемый контракт, когда ручки появятся:
 *   requestEmailChange → POST /profile/email { email } — письмо со ссылкой
 *   confirmEmailChange → GET  /profile/email/status   — перешли ли по ссылке
 *
 * Обе умеют бросать: экраны показывают ошибку и остаются на месте.
 */

/** Отправить письмо со ссылкой подтверждения на новый адрес. */
export const requestEmailChange = (email: string): Promise<void> => {
  void email;
  return Promise.resolve();
};

/** Проверить, что пользователь перешёл по ссылке из письма. */
export const confirmEmailChange = (): Promise<void> => Promise.resolve();
